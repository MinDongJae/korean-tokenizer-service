services:
  - type: web
    name: korean-tokenizer
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 8080
    healthCheckPath: /health
    autoDeploy: true
